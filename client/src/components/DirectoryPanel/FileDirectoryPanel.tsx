import FileDirectory from "./FileDirectory";

const FileDirectoryPanel = () => {
  return (
    <div className="py-2 h-full overflow-y-scroll">
      <FileDirectory />
    </div>
  );
};

export default FileDirectoryPanel;
